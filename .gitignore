.idea/
.vscode/

# Java / Maven
target/
*.class
*.jar
*.war

# Python
.venv/
venv/
# A root-level virtual environment can be created by Linux tooling. Its
# interpreter shims and installed packages are machine-specific, not source.
/bin/
/lib/
/lib64/
/pyvenv.cfg
__pycache__/
.pytest_cache/
*.py[cod]

# Node / frontend
node_modules/
dist/
build/

# Local secrets and runtime files
.env
.env.*
!.env.example
.local-secrets/
.local-logs/
.local-run/
*.log
logs/
.local-storage/
output/
tmp/
.playwright-cli/
docs/superpowers/plans/

# Local agent planning state and interrupted Maven output must not enter
# project history; the generated files can be recreated from tracked source.
.zcode/
/backend-java/target-incomplete-*/

# Windows reserves this device name, so it cannot be represented safely in a
# cross-platform Git checkout.
/NUL

# Local work folders and generated previews
.resume_work/
.tmp/
.trae/
artifacts-*.png
artifacts-*.pdf
tmp-*.txt

# OS
Thumbs.db
.DS_Store
