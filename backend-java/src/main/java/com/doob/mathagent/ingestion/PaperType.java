package com.doob.mathagent.ingestion;

/** Supported examination-source categories; every import must declare one explicitly. */
public enum PaperType {
    GAOKAO, ZHONGKAO, MOCK_EXAM, COMPETITION, GENERIC
}
