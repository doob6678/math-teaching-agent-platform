<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doob.mathagent.agent.mapper.AgentWorkerTaskOutboxEventMapper">
    <!-- Batch claim: one UPDATE replaces the previous per-row CAS loop (~2N round trips -> 2).
         Single-table UPDATE ... ORDER BY created_at LIMIT n is supported by MySQL; the PENDING predicate
         in WHERE is what keeps concurrent claimants mutually exclusive. -->
    <update id="claimBatchPending">
        UPDATE agent_worker_task_outbox_event
        SET status = 'PUBLISHING',
            locked_by = #{publisherId},
            publish_lease_until = #{leaseUntil},
            publish_attempt = publish_attempt + 1
        WHERE status = 'PENDING'
          AND next_attempt_at &lt;= #{now}
        ORDER BY created_at ASC
        LIMIT #{limit}
    </update>

    <select id="selectClaimed" resultType="com.doob.mathagent.agent.entity.AgentWorkerTaskOutboxEventEntity">
        SELECT *
        FROM agent_worker_task_outbox_event
        WHERE status = 'PUBLISHING'
          AND locked_by = #{publisherId}
        ORDER BY created_at ASC
    </select>

    <select id="selectOldestUnpublished" resultType="com.doob.mathagent.agent.entity.AgentWorkerTaskOutboxEventEntity">
        SELECT *
        FROM agent_worker_task_outbox_event
        WHERE status IN ('PENDING', 'PUBLISHING')
        ORDER BY created_at ASC
        LIMIT 1
    </select>
</mapper>
