name: math-agent-rag

services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: local_mysql_7f9b1cc8a2d44529b6e481d5
      MYSQL_DATABASE: math_agent_rag
    command: ["mysqld", "--character-set-server=utf8mb4", "--collation-server=utf8mb4_0900_ai_ci"]
    ports: ["3307:3306"]
    volumes:
      - mysql-data:/var/lib/mysql
      - ./backend-java/src/main/resources/db:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 10s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    ports: ["6380:6379"]
    volumes: ["redis-data:/data"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 12

  rabbitmq:
    image: rabbitmq:4-management
    restart: unless-stopped
    ports:
      - "5673:5672"
      - "15673:15672"
    volumes: ["rabbitmq-data:/var/lib/rabbitmq"]
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 20

  milvus-etcd:
    image: quay.io/coreos/etcd:v3.5.18
    restart: unless-stopped
    environment:
      ETCD_AUTO_COMPACTION_MODE: revision
      ETCD_AUTO_COMPACTION_RETENTION: "1000"
      ETCD_QUOTA_BACKEND_BYTES: "4294967296"
      ETCD_SNAPSHOT_COUNT: "50000"
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls=http://0.0.0.0:2379 --data-dir /etcd
    volumes: ["milvus-etcd-data:/etcd"]
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 10s
      timeout: 5s
      retries: 20

  milvus-minio:
    image: minio/minio:RELEASE.2024-12-18T13-15-44Z
    restart: unless-stopped
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: local_minio_1e3d5c7a9b2f4d6e8c0a
    command: minio server /minio_data --console-address ":9001"
    volumes: ["milvus-minio-data:/minio_data"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 20

  milvus:
    image: milvusdb/milvus:v2.6.11
    restart: unless-stopped
    command: ["milvus", "run", "standalone"]
    security_opt: ["seccomp:unconfined"]
    environment:
      ETCD_ENDPOINTS: milvus-etcd:2379
      MINIO_ADDRESS: milvus-minio:9000
      MINIO_ACCESS_KEY_ID: minioadmin
      MINIO_SECRET_ACCESS_KEY: local_minio_1e3d5c7a9b2f4d6e8c0a
      COMMON_SECURITY_AUTHORIZATIONENABLED: "true"
      COMMON_SECURITY_DEFAULTROOTPASSWORD: local_milvus_8f2c6a4d1e9b3f7c5a0d
    ports:
      - "19531:19530"
      - "9092:9091"
    volumes: ["milvus-data:/var/lib/milvus"]
    depends_on:
      milvus-etcd: {condition: service_healthy}
      milvus-minio: {condition: service_healthy}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9091/healthz"]
      interval: 15s
      timeout: 5s
      start_period: 30s
      retries: 20

  ai-worker:
    build: ./ai-worker-python
    restart: unless-stopped
    # Luna page recognition is executed through this worker's Docker network. Docker Desktop's inherited WSL
    # resolver cannot resolve the configured OpenAI relay CNAME chain reliably, so use the same verified resolvers
    # as the backend instead of allowing visual ingestion to block before the provider connection is established.
    dns:
      - 8.8.8.8
      - 1.1.1.1
    # WSL2 exposes the Windows NVIDIA driver through CDI; this makes the local
    # BGE, reranker, and CLIP models use the same CUDA device inside the worker.
    gpus: all
    environment:
      MATH_AGENT_WORKER_API_KEY: local_worker_internal_key
      MATH_AGENT_TOOL_BROKER_BASE_URL: http://backend:8080
      MATH_AGENT_AGENT_WORKER_SHARED_KEY: local_agent_worker_shared_key
      MATH_AGENT_DB_HOST: mysql
      MATH_AGENT_DB_PORT: 3306
      MATH_AGENT_DB_USERNAME: root
      MATH_AGENT_DB_PASSWORD: local_mysql_7f9b1cc8a2d44529b6e481d5
      MATH_AGENT_DB_NAME: math_agent_rag
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_BASE_URL: https://api.openai.com/v1
      # 禁止恢复 DashScope embedding；教材向量、重排和图像向量必须始终留在只读挂载的本地模型中。
      # Do not restore DashScope embeddings; text vectors, reranking, and CLIP must remain on local read-only models.
      MATH_AGENT_EMBEDDING_PROVIDER_ORDER: local_bge_embedding
      MATH_AGENT_RERANK_PROVIDER_ORDER: local_bge_reranker
      MATH_AGENT_CLIP_PROVIDER_ORDER: local_clip
      MATH_AGENT_EMBEDDING_DIMENSION: 512
      MATH_AGENT_LOCAL_CLIP_MODEL_PATH: /models/damo/multi-modal_clip-vit-large-patch14_zh
      MATH_AGENT_LOCAL_RERANK_MODEL_PATH: /models/BAAI/bge-reranker-v2-m3
      MATH_AGENT_LOCAL_TEXT_EMBEDDING_MODEL_PATH: /models/BAAI/bge-small-zh-v1.5
      # GPU is an optional local accelerator. CPU keeps the same read-only local BGE/reranker/CLIP contract on
      # Docker Desktop installations without NVIDIA CDI; operators can set these three values to cuda explicitly.
      MATH_AGENT_LOCAL_CLIP_DEVICE: cpu
      MATH_AGENT_LOCAL_RERANK_DEVICE: cpu
      MATH_AGENT_LOCAL_TEXT_EMBEDDING_DEVICE: cpu
      MATH_AGENT_USAGE_LEDGER_PATH: /app/data/usage/provider-usage.jsonl
    ports: ["8092:8091"]
    volumes:
      - worker-data:/app/data
      - /mnt/d/ModelScope/models:/models:ro
    depends_on:
      mysql: {condition: service_healthy}

  backend:
    build:
      context: .
      dockerfile: backend-java/Dockerfile
    restart: unless-stopped
    # Docker Desktop's inherited WSL resolver cannot resolve the configured OpenAI relay CNAME chain. Google DNS
    # resolves it from the same host network and keeps provider calls inside the container deterministic.
    dns:
      - 8.8.8.8
      - 1.1.1.1
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    ports: ["8080:8080"]
    volumes:
      - backend-data:/app/data
      # Fixed WSL source path for the real 2008–2024 paper collection. This is deliberately not an environment
      # interpolation: a run must show exactly which operator-approved source directory it mounted.
      - "/mnt/c/Users/doob/Desktop/个人资料/高考真题/数学高考真题试卷【点进去分开保存，链接长期在】/【新·PDF版】2008-2024·高考数学真题/版本3：数学（按年份分类）2008-2024:/app/data/gaokao-input:ro"
      - "./output/gaokao-evidence:/app/data/gaokao-evidence"
      - /mnt/c/Users/doob/Desktop/个人资料/高中数学/下载课本代码/tchMaterial-parser-main/tchMaterial-parser-main/processed_books_section_shadow_all_mini_b4:/app/data/textbooks:ro
      - /mnt/c/Windows/Fonts/simhei.ttf:/app/fonts/cjk.ttf:ro
    depends_on:
      mysql: {condition: service_healthy}
      redis: {condition: service_healthy}
      rabbitmq: {condition: service_healthy}
      milvus: {condition: service_healthy}
      ai-worker: {condition: service_healthy}

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports: ["5173:80"]
    depends_on:
      backend: {condition: service_healthy}

volumes:
  mysql-data:
  redis-data:
  rabbitmq-data:
  milvus-etcd-data:
  milvus-minio-data:
  milvus-data:
  worker-data:
  backend-data:
