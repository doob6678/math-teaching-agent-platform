# 阶段 07：切题题号边界提示

状态：已完成（确定性文本提示；不等同于完整视觉切题验收）。

`QuestionNumberDetector` 只识别行首顶层题号：`17．`、`17.(2)`、`第18题`。它不会将 `（1）` 子问或“2024 年”识别为新题。题号仅用于辅助分界；实际 occurrence 身份仍依赖页码与 `QuestionRegion`，从而避免双栏、跨页、选做题与重复编号造成错误合并。

`QuestionNumberDetectorTest` 先以类不存在红灯，随后与全部核心测试合计 19 项通过。没有输入真题、页面坐标或模型调用，因此不能把此阶段标记为完整视觉切题完成。
