# 讲义架构验收清单

本清单用于每一次教师版、学生版、课堂讲解版或其 PDF 的真实交付验收。未执行的项目保持未勾选；不得因已有历史产物、旧报告、旧截图或旧 PDF 将本次运行标记为通过。

## 本次运行记录

- [ ] 记录本次运行标识、执行时间和验收人。
- [ ] 记录本次来源证据位置、完整来源文件名和来源 SHA-256。
- [ ] 确认记录对应本次运行输入，不是历史 `output`、缓存或先前发布物。
- [ ] 记录 provider 首个 chunk 延迟、接收 chunk 数、private checkpoint flush 次数、最大未落盘字符和 flush 延迟；确认终态 force flush 成功。
- [ ] 确认 handout SSE 事件游标可连续重连，且公共事件不含 raw prompt、raw completion、答案、教师批注、trace、来源内部标识、assetId、路径、URL 或凭据。
- [ ] 记录本次运行的 checkpoint outage 次数；若发生 outage，确认返回可恢复错误且未覆盖最后有效 checkpoint。

## 协议状态与格式契约

- [ ] 审阅本次讲义导出和 MCP workflow 变更：任务/阶段状态必须复用权威枚举或集中常量；动态返回的状态值只能通过该枚举或常量映射产生，业务逻辑中不得散落硬编码状态字符串或字符串比较。
- [ ] 审阅外部协议兼容性：导出格式不是状态码，格式别名和规范外部值必须在单一映射中定义；不得在多个业务分支重复处理历史协议值。

## 内容与隔离

- [ ] 审阅调用链：可见教师、学生、课堂讲解正文仅由 AI/Python Writer 产生。
- [ ] 审阅 Java/前端变更：不存在固定教学文案、题目/主题分支、默认标题、答案、提示或自动生成的图像内容。
- [ ] 抽检学生版：没有最终答案、教师批注、推理轨迹或内部来源标识。
- [ ] 确认 RAG 后的 AI 仅读取或检索本次运行已授权文档，未提供任意文件系统或 shell 访问。

## 图片与来源

- [ ] 每张展示图片均能映射到本次运行授权的真实来源资产；无有效选择的区块不显示图片。
- [ ] 禁止使用assetID，禁止使用，已经过时！！！禁止返回AI assetID
- [ ] **高考图片资格门禁：只允许使用完整来源目录 `output/math-paper-corpus/<完整来源文件名>/figures/` 中由题目级 manifest 和 `questions/*.md` 明确绑定、明确引用的 `figures/` 资产。`page-images/` 仅用于页级定位、OCR、版面审计和来源复核，严禁进入 AI 选图、授权物化、讲义 Markdown、XeLaTeX 或任何 PDF；严禁对页面进行二次切分来生成讲义图片。题目没有合格 `figures/` 引用时，高考题只保留文字、不显示图片。**
- [ ] **教材图片隔离：教材检索返回的 `sourcePageImage`、页面图片 URI 和教材页图资产不得进入教师版、学生版或课堂讲解版 PDF；讲义图片只能来自已授权飞书原始图片或合格高考 `figures/`。**
- [ ] 高中来源链路可复核 GPU PaddleOCR/版面资产、同源 Terra/Luna 页级转写、完整来源名目录、全文 Markdown、逐题 Markdown 和复制的来源资产。
- [ ] 在授权分块和向量入库前完成上述来源目录与资产发布，且入库记录能回溯到来源证据。

## PDF 交付

- [ ] 检查中文字符、数学公式和学生作答留白；公式未出现乱码、截断或歧义写法。
- [ ] 审阅本次生成 PDF 的全部页面视觉结果，确认正文、图片、页眉页脚和分页无重叠、裁切或空白异常。
- [ ] 保存本次 PDF 的真实编译/渲染结果与必要审阅证据；历史 PDF 或截图不计入本项。
- [ ] 所有必填项均有本次证据后，才将交付状态标记为通过。

## 2026-08-23 抛物线恢复运行证据

- 工作流：`8f473acc-6b43-4197-b063-11a8df9c4752`；运行目录：`output/acceptance/handout-mcp/parabola-three-source-fixed-runtime-final-rerun/`。
- 这是同一失败工作流的恢复，`taskCreationPosts=0`；worker 无缓存重建后 `healthy`、`RestartCount=0`，按 15 秒轮询至 `COMPLETED`。
- 本次状态确认三源非零：公开教材 3、飞书教师资料 10、规范高考题库 2；`AI_DRAFT` 和 `LATEX_HANDOUT` 均完成。
- XeLaTeX/Poppler 产物：教师版 7 页、学生版 4 页、16:10 课堂讲解版 4 页；全部页面 PNG 已保存并逐页检查为非空，文本字符数分别为 11989、2526、4197。
- 学生版文本隔离扫描未命中答案、教师批注、trace、内部来源标识、路径或 URL；课堂讲解版非空，中文和公式可见。
- 本次不勾选“完整来源透明引用”和“fresh-run 原始检索落盘”相关项目：恢复 runner 未重新写出检索响应，教师 PDF 文本也未检出 `feishu://`/`gaokao://`。因此该运行不能宣称清单最终全通过；下一次 fresh 任务必须补齐透明相对引用和来源审计证据。
